name: Scan & Go CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Pure Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scango_python/requirements.txt

      - name: Validate Database & Seed Data
        run: |
          python scango_python/database.py
